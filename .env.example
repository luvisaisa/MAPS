# PostgreSQL Database Configuration for RA-D-PS
# Copy this file to .env and fill in your values

# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ra_d_ps
DB_USER=ra_d_ps_user
DB_PASSWORD=your_secure_password_here

# Connection Pool Settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# SSL/TLS Settings (optional)
DB_SSL_MODE=prefer
# DB_SSL_CERT=/path/to/client-cert.pem
# DB_SSL_KEY=/path/to/client-key.pem
# DB_SSL_ROOT_CERT=/path/to/server-ca.pem

# Debug Settings
DB_ECHO_SQL=false
DB_POOL_PRE_PING=true

# Application Settings
ENVIRONMENT=development
LOG_LEVEL=INFO

# =====================================================================
# Supabase Configuration
# =====================================================================

# Supabase Project URL (find in Supabase Dashboard → Settings → API)
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous Key (find in Supabase Dashboard → Settings → API)
SUPABASE_KEY=your-anon-key-here

# Supabase Database URL (PostgreSQL connection string)
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Find in: Supabase Dashboard → Settings → Database → Connection string → URI
SUPABASE_DB_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# Note: You can use either SUPABASE_DB_URL or the standard DB_* variables above
# SUPABASE_DB_URL is preferred for Supabase-hosted PostgreSQL
